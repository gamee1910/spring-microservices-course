info:
  name: Create Customer
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/api/accounts/create
  body:
    type: json
    data: |-
      {
        "name": "game",
        "email": "hhhau1910@gmail.com",
        "mobileNumber": "08864315592"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

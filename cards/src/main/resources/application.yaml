server:
  port: 9000
spring:
  datasource:
    username: sa
    password: ''
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
